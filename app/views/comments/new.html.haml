.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message
  = form_with(model: @comment, local: true, url: board_task_comments_path) do |f|
    %h2 New Comment
    %div
      = f.text_area :comment
    = f.submit 'Submit', class: 'btn-primary'
